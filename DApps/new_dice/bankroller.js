/* global DCLib */

// (function(){
window.MyDApp_debug = (function () {
  var myDApp = new DCLib.DApp({slug : 'new_dicegame'})

  // Banroller side code
  // console.log(myDApp)

  return myDApp
})()
